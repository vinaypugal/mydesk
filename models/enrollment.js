import mongoose from "mongoose";
let Schema = mongoose.Schema;

let enrollment = new Schema({
  
});

mongoose.models = {};

let Enrollment = mongoose.model("Enrollment", enrollment);

export default Enrollment;
